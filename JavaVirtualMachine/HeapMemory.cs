using System;
using System.Collections.Generic;
using System.Text;

namespace JavaVirtualMachine
{
    class HeapMemory : HeapItem
    {
        public HeapMemory(long size)
        {
            //data = new Memory<byte>(new byte[size]);
        }
    }
}
